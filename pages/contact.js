// Modules
import PropTypes from 'prop-types'

// Components

// PropTypes
const propTypes = {}
const contact = () => (
  <div>contact</div>
)

contact.propTypes = propTypes
export default contact
